# tinyRAG_pipeline dependencies
# Install: pip install -r requirements.txt

# ── Core ──────────────────────────────
typer>=0.9.0
pydantic>=2.0
pyyaml>=6.0
loguru>=0.7
jieba>=0.42
fastembed>=0.2
numpy>=1.24
tqdm>=4.65
tiktoken>=0.7

# ── MCP Server (optional) ─────────────
# Uncomment to enable MCP adapter:
mcp>=0.9

# ── Dev (optional) ────────────────────
pytest>=7.0
ruff>=0.1
