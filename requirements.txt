# tinyRAG_pipeline dependencies
# Install: pip install -r requirements.txt

# ── Core ──────────────────────────────
typer>=0.24.0
pydantic>=2.0
pyyaml>=6.0
loguru>=0.7.0
jieba>=0.42
fastembed>=0.7.0
numpy>=1.24
tqdm>=4.65
tiktoken>=0.7
sqlite-vec>=0.1.0

# ── MCP Server (optional) ─────────────
# Uncomment to enable MCP adapter:
mcp>=0.9
httpx>=0.28.0
sse-starlette>=3.0.0
uvicorn>=0.42.0

# ── Dev (optional) ────────────────────
pytest>=7.0
ruff>=0.1
